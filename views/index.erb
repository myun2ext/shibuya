<% path = @node_path.map { |node| "/" + node.path }.join("") %>

<% @nodes.each do |node| %>
  <a href="/nodes<%= path + "/" + node.path %>">
    <div class="node-item" style="background-color: #<%= node.color %>; color: #fff;">
      <h2><%= node.name %></h2>
    </div>
  </a>
<% end %>
